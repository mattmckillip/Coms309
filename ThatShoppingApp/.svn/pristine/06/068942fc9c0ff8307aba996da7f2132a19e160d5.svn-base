package com.example.thatshoppingapp.home;

import android.view.View;
import android.view.ViewGroup;

public interface HomeAddon
{
	//Should return a view that will be added under the page's category.
	//Return null if you only want to link.
	public View getHomeAddon(ViewGroup container);
	
	//Should return an icon to be added next to the category name.
	public int getIcon();

}
